import AdminPresentersClient from "./client";

export const metadata = {
    title: "Manage Presenters | Admin | Pie Radio",
    description: "Create, edit, and manage radio presenters",
};

export default function AdminPresentersPage() {
    return <AdminPresentersClient />;
}
